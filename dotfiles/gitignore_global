# ~/.gitignore_global — git's global excludesfile for darwin (set up 2026-06-23).
# Wire it:   git config --global core.excludesfile ~/.gitignore_global
# Canonical, reconstructable copy + install note: ~/Code/darwin-mac-ops/dotfiles/gitignore_global
# A repo can override any line locally with a negation, e.g.  !keep-this.bak

# --- editor / OS cruft ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
*.swp
*~

# --- backups: BOTH the plain glob AND timestamped variants (foo.py.bak.20260623-031053) ---
# The timestamped form does NOT end in ".bak", so it SLIPS a bare "*.bak" — this bit strike-zone
# twice (a dirty repo tripping the HANDOFF-GATE + junk rsync'd to the runtime box). Belt-and-suspenders:
# ignore both forms everywhere. See lesson 2026-06-23-trap-timestamped-backup-foo-py-bak.
*.bak
*.bak.*
*.orig
