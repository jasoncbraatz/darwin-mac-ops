# portability-guard.allow — darwin-mac-ops (smDrainDesk-08, 2026-09-07)
# The guard reads THIS file from the repo it is committing in (cd toplevel; ALLOW=portability-guard.allow),
# not ~/Scripts', so the `*.plist` allowance over there never applied here. That went unnoticed
# until core.hooksPath became global on 2026-09-07 and the first new plist after it was refused.
# Format: <glob> | <why this one is deliberately darwin-only>
*.plist                     | launchd. macOS-only by construction (launchd does not expand ~); feynman schedules with systemd.
launchagents/*              | ditto — these are darwin's limbs, not portable tools.
# retired-plists/* was copied over from ~/Scripts's allowlist and retired 2026-09-07
# by feynmanSync-08: this repo tracks no such directory, so the line excused nothing
# while reading as a deliberate ruling. Re-add it the day this repo grows one.
handoff-reference-integrity.py | it is deliberately about BOTH boxes: the tool must know the macOS home prefix and the Linux one BY NAME, to tell an unresolvable path from the other box's path -- which is a routing fact and must never go red. Green on darwin and feynman; control 3/3t proves the classification flips with the RUNNING box, not with a hardcoded username. (The reason cannot spell the prefix out: check 1 consults this allowlist but does not exempt it, so an explanation quoting the pattern makes this file itself the violation.)
docs/handoffs/*.md          | lane handoffs (sweeper-01, 2026-09-10). claude-blackbook/scripts/smdrain-lane.py writes the ruler's ABSOLUTE path on the arming box into verify_cmd/ruler_files, and rail.py digests exactly those paths at rail-on (ff86a5b: DECLARED, never inferred) -- a relative path would be a moved ruler. Same class as ~/Scripts's docs/* line: a frozen record of what the desk saw, not a tool that runs on both boxes. First refusal: sweepGates, the first smdrain handoff here since core.hooksPath went global on 09-07.
